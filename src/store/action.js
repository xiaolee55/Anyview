/*
通过mutation间接更新state的多个方法的对象
*/
import {
    LOGIN
} from './mutation-types'
import {
    reqUser,
    reqSchoolList
} from '../api'

export default {
    async getUser({commit, state}) {
        
    }
}